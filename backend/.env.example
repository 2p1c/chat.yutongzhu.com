# Copy to backend/.env and adjust as needed. Defaults match docker-compose.yml.
REDIS_URL=redis://localhost:6379/0
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/chatdb
# Fixed test user for this phase (no real auth).
DEFAULT_USER_ID=user_demo
# Number of recent messages kept in the Redis cache window.
CACHE_RECENT_MESSAGES=10
# Agent container (minimal-agent-ts, see BACKEND_INTEGRATION.md). When running
# Agent in docker on this host, the host port mapping is what Python dials.
AGENT_URL=http://localhost:8765
