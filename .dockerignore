# Used when a build context is the repo root. backend/Dockerfile now builds
# from ./backend (see backend/.dockerignore). Harmless if unused.
.git
.github

**/.venv
**/__pycache__
**/*.pyc
**/.env
**/.DS_Store

instruction/
